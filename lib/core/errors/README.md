# Core Errors

Common error types and error helpers used across features.

## Responsibilities

- Define reusable error models/exceptions.
- Provide shared mapping/formatting utilities (when applicable).
- Provide shared datasource wrappers such as `SafeDatasource` for centralized
  logging, Crashlytics reporting, and exception normalization.
