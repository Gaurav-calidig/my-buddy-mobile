# Core Services

Reusable service classes used by multiple features (for example sharing, analytics, helpers).

## Responsibilities

- Provide small, feature-agnostic services.
- Avoid UI code in services when possible.


## Detailed Docs

- Remote Config implementation: `lib/core/services/README_remote_config.md`
