# Core Config

Build-time and runtime configuration for the app (for example feature flags and environment-driven switches).

## Responsibilities

- Centralize configuration primitives that multiple modules rely on.
- Keep config access simple and predictable (prefer small, typed APIs).

